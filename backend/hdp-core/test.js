const HDPCore = require('./index');

async function test() {
    const hdp = new HDPCore({ debug: true });
    
    console.log('\n╔══════════════════════════════════════════════════════════════╗');
    console.log('║              🧪 تست ماژول اصلی HDP ONE                       ║');
    console.log('╚══════════════════════════════════════════════════════════════╝\n');
    
    const tests = [
        'سلام چطوری؟',
        'ترافیک چطوره؟',
        'قیمت طلا چند است؟',
        'به قشم سفر کنم',
        'ترجمه سلام به بندری'
    ];
    
    for (const test of tests) {
        console.log(`📝 ورودی: "${test}"`);
        const result = await hdp.process(test);
        console.log(`   🎯 Intent: ${result.intent}`);
        console.log(`   💬 پاسخ: ${result.response}\n`);
    }
    
    console.log('📊 آمار:');
    console.log(hdp.getStats());
    console.log('\n✅ تست ماژول اصلی با موفقیت انجام شد!');
}

test();
